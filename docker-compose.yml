services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: jobapply
      POSTGRES_USER: jobapply
      POSTGRES_PASSWORD: jobapply
    ports:
      - "5432:5432"
    volumes:
      - jobapply-postgres:/var/lib/postgresql/data

volumes:
  jobapply-postgres:
